import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        textAlign: "center",
        color: "#000000",
        fontSize: 26,
        marginTop: 60
    }
});
