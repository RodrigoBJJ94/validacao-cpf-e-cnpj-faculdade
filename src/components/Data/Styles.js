import { StyleSheet } from "react-native";

export default StyleSheet.create({
    viewData: {
        marginTop: 70,
        marginLeft: 10,
        marginRight: 10,
    },
    textData: {
        color: "#000000",
        fontSize: 16,
        textTransform: "capitalize",
        marginBottom: 6
    }
});
