import { StyleSheet } from "react-native";

export default StyleSheet.create({
    viewButton: {
        marginTop: 40,
        alignItems: "center",
    },
    button: {
        backgroundColor: "#911fff",
        padding: 15,
         borderRadius: 4
    },
    textButton: {
        fontSize: 20,
        color: "#000000"
    }
});
